задача написать физический движок для 2d sidescroller на f# используя строго функциональное программирование. план на начало:
1) написать модуль для парсинга нажатий клавиш - собирает за какой-то промежуток список нажатий - отпусканий клавиш и потом по вызову метода возвращает их массив
2) написать структуры физических объектов на которые не будет действовать физика (но с ними можно сталкиваться) - с хитбоксами-прямоугольниками
3) написать структуры объектов на которые действиует физика (проджектайлы, игроки, враги) - пока тоже прямоугольники, у них есть скорость и положение
4) написать фукционал стояния на платформе - если нижняя граница хитбокса пересекается с статичной платформой то объект поднимается пока не перестанет с ней пересекаться, написать управление с движением влево-вправо с помощью клавиш.


Хотелки Паши(надо - сам иди делай)
- От Всех:
1) Введите какой-нибудь файлик(можно юзать этот) куда вы будете писать название методов и по хорошему небольшое описание для них, которые вы хотите чтобы я использовал, а то читать весь ваш код я не хочу
2) Скидвайте типы, которые могут быть использованы не только в вашем коде в Types.fs В Вовином проекте.
- От Кирилла
1) Сделать обработку Stage(или Scene), включающую в себя объекты игры(отрисовать их на экране). Конкретная реализация Stage где-то была, сам не нашел, но вроде мы это где-то обсудили. В идеале это должна быть одна функция, куда я передаю массив(или мапу, не помню, что именно мы решили) всех объектов на экране и ты их отрисовываешь
2) Возможность добавлять кнопки(ну по-хорошему я глянул, вроде это сделано с помощью настройки isMouseClick и область в которую нужно кликнуть, так что в теории это я могу и должен делать, но ты глянь еще на всякий). Но в любом случае мне нужна функция, которая позволяет отрисовать прямоугольник с заданной текстурой на экране
- От Вовы
1) Тот же Stage, который после передачи ты обрабатываешь и отдаешь мне новое состояние + значение триггеров.
2) По игровым элементам, поведение которых нужно реализовать: мобы(действует гравитация, 2 вектора скорости, 2 координаты), снаряды(не действует гравитация + скорость + координаты), стены/платформы - координаты + коллизия. Хотя в теории коллизию можно со всеми сделать. У каждого возможность добавлять триггер на пересечение хитбоксов.

Пока вроде все, там поглядим